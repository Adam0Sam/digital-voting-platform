// import { createAsyncThunk } from '@reduxjs/toolkit';
// import { sliceName } from './proposalsSlice';
// import { ProposalData } from '@/types/proposal.type';
// import URI from '@/lib/constants/uri-constants';

// export const createProposal = createAsyncThunk(
//   `${sliceName}/createProposal`,
//   async (proposal: ProposalData) => {
//     return await fetch(`${URI.API}/proposals/create`, {});
//   },
// );
